 - see IE semantic changes (mask broken into prefix len and prefix) - test required here
 - check addTemplate callback to register internal template
 - Use G_DEBUG=gc-friendly G_SLICE=always-malloc to test with valgrind (fixbuf uses glibc)
